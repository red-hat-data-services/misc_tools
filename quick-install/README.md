Note: When using this you will see some "Error" messages. These are normal to see while the install
process creates the required namespaces.  After approximately 30 seconds, your install should proceed.

